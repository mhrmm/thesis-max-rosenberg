import unittestfrom ozone.animals import AnimalNetclass TestAnimalNet(unittest.TestCase):        def test_animal_net(self):        net = AnimalNet()        node = net.get_animal('finch')        print(node)            ## TODOs: extend DirectedGraph to support exploring the taxonomy graph        ## TODO: write tests to make sure you can add AnimalVertex objects to    ## a set, a list, compare two of them, see if one is in a graph,  etc.